# Copy to .env and set values (never commit .env)
ADMIN_PASSWORD=change-me

# Optional: fine-grained PAT with Contents read/write on this repo only.
# Used so live Admin can commit image/price updates (also set as GitHub secret).
# ADMIN_GITHUB_TOKEN=github_pat_...
